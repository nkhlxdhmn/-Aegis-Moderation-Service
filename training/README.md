# Training

This folder documents the optional model training pipeline without bundling private datasets or model artifacts.

Expected local-only folders are ignored by Git:

- `training/datasets/`
- `training/weights/`
- `training/outputs/`
- `training/checkpoints/`

Add reproducible scripts and public datasets only after license and privacy review.
