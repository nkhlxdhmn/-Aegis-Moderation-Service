# api

This directory is reserved for the standalone platform structure. The current v1.0.0 implementation keeps production code in ackend/, pipeline/, models/, and rontend/ while preserving clear extension points for this layer.
